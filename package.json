{
  "name": "@vf/academy-root",
  "version": "1.0.0",
  "private": "true",
  "main": "index.js",
  "license": "MIT",
  "workspaces": {
    "packages": [
      "packages/**/*"
    ],
    "nohoist": [
      "**/react",
      "**/react-dom"
    ]
  },
  "scripts": {
    "asbuild": "lerna run --stream asbuild",
    "bootstrap": "git clean -xdf && yarn && lerna bootstrap",
    "bootstrap:force": "lerna clean --yes && rm -rf node_modules && yarn bootstrap",
    "build": "lerna run --stream build",
    "dev": "yarn lerna run dev --scope=@vf/app",
    "lerna:version": "yarn lerna version --conventional-commits",
    "build:app": "lerna run --stream build:app"
  },
  "volta": {
    "node": "16.17.0",
    "yarn": "1.22.19"
  },
  "devDependencies": {
    "assemblyscript": "^0.12.3",
    "lerna": "^5.5.1"
  }
}
